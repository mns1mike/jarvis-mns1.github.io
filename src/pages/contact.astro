---
import BaseLayout from "@layouts/BaseLayout.astro";
import PageHero from "@components/PageHero.astro";
import { site } from "@data/site";
---

<BaseLayout title="Contact MNS1 Express | Drivers & Shippers" description="Questions about driving for MNS1, shipping with us, or anything else? Call, email, or fill out the form. Real people answer.">
  <PageHero
    eyebrow="Contact"
    title="Get in Touch"
    lead="Questions about driving for MNS1, shipping with us, or anything else? Call, email, or fill out the form. Real people answer."
    centered
  />
  <section class="section">
    <div class="wrap split">
      <div>
        <p class="eyebrow">Reach Us</p>
        <h2>Direct Lines</h2>
        <div class="grid grid--cards">
          <article class="card"><h3>Phone</h3><p><a class="text-link" href={site.phoneHref}>{site.phoneDisplay}</a></p></article>
          <article class="card"><h3>Address</h3><p>14458 S Depot Dr<br />Plainfield, IL 60544</p></article>
          <article class="card"><h3>Email</h3><p><a class="text-link" href={`mailto:${site.email}`}>{site.email}</a></p><p><a class="text-link" href={`mailto:${site.recruitingEmail}`}>{site.recruitingEmail}</a></p></article>
        </div>
      </div>
      <aside class="panel">
        <p class="eyebrow">Send a Message</p>
        <h3>Contact Form</h3>
        <form class="contact-form" action={`mailto:${site.email}`} method="post" enctype="text/plain">
          <label><span>Name</span><input type="text" name="name" placeholder="Name" /></label>
          <label><span>Email</span><input type="email" name="email" placeholder="Email" /></label>
          <label><span>Phone</span><input type="tel" name="phone" placeholder="Phone" /></label>
          <label><span>Message</span><textarea name="message" rows="5" placeholder="Message"></textarea></label>
          <button type="submit">Send message</button>
        </form>
      </aside>
    </div>
  </section>
  <section class="section section--panel">
    <div class="wrap panel">
      <h2>Ready to Drive?</h2>
      <p>Skip the form — apply directly in 4 minutes.</p>
      <p><a class="text-link" href="/apply/">Open application page</a></p>
    </div>
  </section>
</BaseLayout>
