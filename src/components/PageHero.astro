---
type Props = {
  eyebrow: string;
  title: string;
  lead: string;
  accent?: string;
  centered?: boolean;
};

const { eyebrow, title, lead, accent, centered = false } = Astro.props;
const accentIndex = accent ? title.indexOf(accent) : -1;
const beforeAccent = accentIndex >= 0 ? title.slice(0, accentIndex).trimEnd() : title;
const afterAccent = accentIndex >= 0 ? title.slice(accentIndex + accent.length).trimStart() : "";
---

<section class="hero page-hero" style={centered ? "text-align: center" : undefined}>
  <div class="wrap">
    <p class="eyebrow">{eyebrow}</p>
    <h1 class="display-title">
      {accentIndex >= 0 ? (
        <>
          {beforeAccent}
          {beforeAccent && " "}
          <span>{accent}</span>
          {afterAccent && <> {afterAccent}</>}
        </>
      ) : (
        title
      )}
    </h1>
    <p class="lead" style={centered ? "margin-inline: auto" : undefined}>{lead}</p>
    <slot />
  </div>
</section>
